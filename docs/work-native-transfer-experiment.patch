--- a/src/provider_support.rs
+++ b/src/provider_support.rs
@@ -78,13 +78,34 @@
 
 /// One task of job kind 'Infra.BlockJobs', off the turn: decode it, run the
 /// function `aver.toml` bound, and hand the answer back as a value.
-fn work_body_0(__task: ProviderValue) -> Result<ProviderValue, String> {
-let __registry = registry();
-let __task = ProviderCodec::from_provider_value(__task, __registry, "Infra.BlockJobs", None)
-.map_err(|why| format!("work: job kind 'Infra.BlockJobs' was handed an unusable task: {}", why))?;
-let __produced = crate::aver_generated::domain::block_work_job::run(&__task);
-__produced.into_provider_value(__registry, "Infra.BlockJobs")
-.map_err(|why| format!("work: job kind 'Infra.BlockJobs' produced an unusable result: {}", why))
+// EXPERIMENT ONLY: measure removing the Work value-tree roundtrip.
+// Not a production capability binding or a compiler change.
+fn work_body_0(task: ProviderValue) -> Result<ProviderValue, String> {
+    let ProviderValue::Resource(resource) = task else { return Err("experiment: expected native task".into()) };
+    let task = resource.downcast_ref::<std::sync::Mutex<Option<crate::aver_generated::infra::block_jobs::Task>>>()
+        .expect("experiment task type").lock().unwrap().take().expect("task once");
+    let answer = crate::aver_generated::domain::block_work_job::run(&task);
+    Ok(ProviderValue::Resource(aver_rt::provider::ProviderResource::new(std::sync::Mutex::new(Some(answer)))))
+}
+
+pub fn work_begin_native(task: crate::aver_generated::infra::block_jobs::Task) -> Result<crate::aver_generated::work::Job, aver_rt::AverStr> {
+    invoke("Infra.BlockJobs", "Infra.BlockJobs.begin", vec![ProviderValue::Resource(aver_rt::provider::ProviderResource::new(std::sync::Mutex::new(Some(task))))], Some("Work.Job"), "Result<Work.Job, String>")
+}
+
+pub fn work_take_native(job: crate::aver_generated::work::Job) -> Result<Option<crate::aver_generated::infra::block_jobs::Reply>, aver_rt::AverStr> {
+    let value = registry().invoke_owned("Infra.BlockJobs.take", vec![encode(job,"Infra.BlockJobs")]).expect("experiment take dispatch");
+    match value {
+        ProviderValue::ResultErr(message) => match *message { ProviderValue::String(s) => Err(s.into()), _ => panic!("experiment error") },
+        ProviderValue::ResultOk(answer) => match *answer {
+            ProviderValue::OptionNone => Ok(None),
+            ProviderValue::OptionSome(value) => match *value {
+                ProviderValue::Resource(resource) => Ok(resource.downcast_ref::<std::sync::Mutex<Option<crate::aver_generated::infra::block_jobs::Reply>>>().expect("experiment reply type").lock().unwrap().take()),
+                _ => panic!("experiment reply")
+            },
+            _ => panic!("experiment option")
+        },
+        _ => panic!("experiment result")
+    }
 }
 
 pub fn preflight_required_providers() -> Result<(), String> { registry().preflight(["Args.get", "Console.error", "Console.print", "Disk.appendBytes", "Disk.appendText", "Disk.delete", "Disk.exists", "Disk.listDir", "Disk.makeDir", "Disk.readBytesAt", "Disk.readText", "Disk.size", "Disk.sync", "Disk.writeBytes", "Disk.writeText", "Domain.Primitives.ripemd160", "Domain.Primitives.sha1", "Domain.Primitives.taprootTweakMatches", "Domain.Primitives.verifySchnorr", "Domain.Primitives.verifySignature", "Infra.BlockJobs.begin", "Infra.BlockJobs.take", "Infra.Kv.applyAll", "Infra.Kv.count", "Infra.Kv.deleteAll", "Infra.Kv.get", "Infra.Kv.getAll", "Infra.Kv.open", "Infra.Kv.putAll", "Process.stopRequested", "Random.int", "Tcp.accept", "Tcp.beginConnect", "Tcp.close", "Tcp.closeDial", "Tcp.closeListener", "Tcp.dialled", "Tcp.listen", "Tcp.peerAddress", "Tcp.readNow", "Tcp.writeNow", "Terminal.clear", "Terminal.disableRawMode", "Terminal.enableRawMode", "Terminal.flush", "Terminal.hideCursor", "Terminal.moveTo", "Terminal.print", "Terminal.readKey", "Terminal.showCursor", "Time.now", "Time.unixMs", "Wait.poll", "Work.cancel"]) }
--- a/src/aver_generated/infra/working/mod.rs
+++ b/src/aver_generated/infra/working/mod.rs
@@ -31,7 +31,7 @@
 }) { return crate::aver_generated::infra::working::stopped(&eye, &*job) } else { match {
     crate::cancel_checkpoint();
     let __provider_arg0: crate::aver_generated::work::Job = (*job).clone();
-    crate::provider_support::invoke::<Result<Option<crate::aver_generated::infra::block_jobs::Reply>, AverStr>>("Infra.BlockJobs", "Infra.BlockJobs.take", vec![crate::provider_support::encode(__provider_arg0, "Infra.BlockJobs")], None, "Result<Option<Reply>, String>")
+    crate::provider_support::work_take_native(__provider_arg0)
 }? { Some(answer @ _) => { return Ok(crate::aver_generated::infra::working::Completed { eye: eye, answer: Some(answer) }) }, None => { __MutualTco1::Serving(eye) } } }
             }
             __MutualTco1::Serving(mut eye @ _) => {
@@ -71,7 +71,7 @@
 }) { Ok(crate::aver_generated::infra::working::Completed { eye: eye.clone(), answer: None }) } else { crate::aver_generated::infra::working::join(eye, &{
     crate::cancel_checkpoint();
     let __provider_arg0: crate::aver_generated::infra::block_jobs::Task = task.clone();
-    crate::provider_support::invoke::<Result<crate::aver_generated::work::Job, AverStr>>("Infra.BlockJobs", "Infra.BlockJobs.begin", vec![crate::provider_support::encode(__provider_arg0, "Infra.BlockJobs")], Some("Work.Job"), "Result<Work.Job, String>")
+    crate::provider_support::work_begin_native(__provider_arg0)
 }?) }
 }
 
--- a/src/aver_generated/infra/chain_state/mod.rs
+++ b/src/aver_generated/infra/chain_state/mod.rs
@@ -328,7 +328,7 @@
     if (crate::aver_generated::domain::block::idOfWholeBlock(&payload) == blockId) { Ok(Some(crate::aver_generated::infra::chain_state::Reading { height: height, blockId: blockId, location: location.clone(), job: {
     crate::cancel_checkpoint();
     let __provider_arg0: crate::aver_generated::infra::block_jobs::Task = crate::aver_generated::infra::block_jobs::Task::Decode(payload);
-    crate::provider_support::invoke::<Result<crate::aver_generated::work::Job, AverStr>>("Infra.BlockJobs", "Infra.BlockJobs.begin", vec![crate::provider_support::encode(__provider_arg0, "Infra.BlockJobs")], Some("Work.Job"), "Result<Work.Job, String>")
+    crate::provider_support::work_begin_native(__provider_arg0)
 }? })) } else { Err(aver_rt::AverStr::from({ let mut __b = { let mut __b = { let mut __b = { let mut __b = { let mut __b = { let mut __b = { let mut __b = aver_rt::Buffer::with_capacity((aver_rt::AverInt::from_i64(146)).to_usize().unwrap_or(0)); __b.push_str(&AverStr::from("the body filed for Height ")); __b }; __b.push_str(&aver_rt::AverStr::from(aver_rt::aver_display(&(height)))); __b }; __b.push_str(&AverStr::from(" hashes to ")); __b }; __b.push_str(&aver_rt::AverStr::from(aver_rt::aver_display(&(crate::aver_generated::domain::block::idOfWholeBlock(&payload))))); __b }; __b.push_str(&AverStr::from(", not the ")); __b }; __b.push_str(&aver_rt::AverStr::from(aver_rt::aver_display(&(blockId)))); __b }; __b.push_str(&AverStr::from(" the Index names; the chain directory needs reindex")); __b })) }
 }
 
